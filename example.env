# Example environment configuration for the Daily Routine project.
# Copy this file to `.env` and replace the placeholder values with your own secrets.

########################################
# Backend (Flask + Socket.IO)
########################################

# Flask runtime configuration
FLASK_ENV=development
FLASK_DEBUG=1
SECRET_KEY=replace-with-a-secure-random-string

# Comma separated list of allowed origins for HTTP and WebSocket calls.
# Use `*` to allow every origin during local development.
CORS_ALLOWED_ORIGINS=http://localhost:5173

########################################
# MongoDB
########################################

# Connection details for your MongoDB deployment.
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB_NAME=daily_routine

# Collection names used by the dashboard's data layer.
MONGODB_USER_COLLECTION=users
MONGODB_HABITS_COLLECTION=habits
MONGODB_HABIT_LOGS_COLLECTION=habit_logs
MONGODB_TASKS_COLLECTION=tasks
MONGODB_EVENTS_COLLECTION=schedule_events
MONGODB_GROUPS_COLLECTION=social_groups

# Optional: provide a separate database for testing or temporary experiments.
# TEST_MONGODB_URI=mongodb://localhost:27017
# TEST_MONGODB_DB_NAME=daily_routine_test

########################################
# Frontend (Vite)
########################################

# Base URLs consumed by the React client for REST and WebSocket traffic.
VITE_API_URL=http://localhost:8000
VITE_SOCKET_BASE_URL=http://localhost:8000
